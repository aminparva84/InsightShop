# Copy this file to .env and fill in your values. Never commit .env.
# Usage: docker run --env-file .env -p 5000:5000 insightshop:latest
# Or:    docker-compose up --build

# --- Required for production (min 32 chars) ---
JWT_SECRET=your-long-random-jwt-secret-at-least-32-characters
SECRET_KEY=your-long-random-flask-secret-key

# --- AWS (for Polly, SES, S3, Secrets Manager) ---
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=us-east-1
# Secret name or ARN in Secrets Manager (app loads it at startup for e.g. GEMINI_API_KEY)
AWS_SECRETS_INSIGHTSHOP=

# --- App run mode ---
FLASK_ENV=production
PYTHONUNBUFFERED=1

# --- Superadmin (production / App Runner): created at first startup if not present ---
# SUPERADMIN_EMAIL=superadmin@insightshop.com
# SUPERADMIN_PASSWORD=Super12345

# --- Optional: Encrypt AI provider API keys at rest (Admin → AI Assistant). Set to a long random string (e.g. 32+ chars). ---
# ENCRYPTION_KEY=your-long-random-encryption-key-at-least-32-characters
